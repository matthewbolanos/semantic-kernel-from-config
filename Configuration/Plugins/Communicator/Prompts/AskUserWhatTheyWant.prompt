name: AskUserWhatTheyWant
description: |
  If you are unsure what the user wants, use this function to ask them to clarify. Do not add steps after this one.
model: gpt3
prompt: |
  The bot is confused by what the user wants. The bot will ask them to clarify.

  User: {{$input}}
  Bot: 
completion:
  temperature: 1
input:
  parameters:
    - name: input
      description: The last message from the user.